# Local dev notes (not for repo)
DEVLOG.md

# Secrets — never commit
.env
.env.*

# Machine-local Claude Code settings — accumulates approved commands, some of
# which embed access tokens. .claude/settings.json (shared allowlist) is fine
# to track; this file is not.
.claude/settings.local.json

# Supabase CLI temp files
supabase/.temp/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
